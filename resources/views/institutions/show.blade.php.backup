@extends('layouts.app')

@section('title', 'Detail Institusi - ' . $institution->name)

@section('content')
<div class="container mx-auto px-6 py-8">
    <!-- Header -->
    <div class="mb-8">
        <div class="flex items-center justify-between">
            <div>
                <nav class="text-sm breadcrumbs mb-4">
                    <ol class="list-none p-0 inline-flex">
                        <li class="flex items-center">
                            <a href="{{ route('dashboard') }}" class="text-blue-600 hover:text-blue-800">Dashboard</a>
                            <svg class="w-3 h-3 mx-3 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
                            </svg>
                        </li>
                        <li class="flex items-center">
                            <a href="{{ route('institutions.index') }}" class="text-blue-600 hover:text-blue-800">Institusi</a>
                            <svg class="w-3 h-3 mx-3 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
                            </svg>
                        </li>
                        <li class="text-gray-500">{{ Str::limit($institution->name, 50) }}</li>
                    </ol>
                </nav>
                <h1 class="text-3xl font-bold text-gray-900">{{ $institution->name }}</h1>
                <p class="text-gray-600 mt-2">
                    <span class="px-2 py-1 bg-blue-100 text-blue-800 text-sm font-medium rounded mr-2">
                        {{ ucfirst($institution->type) }}
                    </span>
                    <span class="px-2 py-1 rounded-full text-sm font-medium
                        @if($institution->is_active) bg-green-100 text-green-800
                        @else bg-red-100 text-red-800
                        @endif">
                        {{ $institution->is_active ? 'Aktif' : 'Tidak Aktif' }}
                    </span>
                </p>
            </div>
            <div class="flex space-x-3">
                <a href="{{ route('institutions.edit', $institution) }}" 
                   class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors duration-200 flex items-center">
                    <i class="fas fa-edit mr-2"></i>
                    Edit
                </a>
                @if($institution->projects->count() === 0)
                    <form action="{{ route('institutions.destroy', $institution) }}" 
                          method="POST" 
                          class="inline"
                          onsubmit="return confirm('Apakah Anda yakin ingin menghapus institusi ini?')">
                        @csrf
                        @method('DELETE')
                        <button type="submit" 
                                class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition-colors duration-200 flex items-center">
                            <i class="fas fa-trash mr-2"></i>
                            Hapus
                        </button>
                    </form>
                @endif
            </div>
        </div>
    </div>

    <!-- Statistics Cards -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <div class="flex items-center">
                <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
                    <i class="fas fa-project-diagram text-blue-600 text-xl"></i>
                </div>
                <div>
                    <div class="text-2xl font-bold text-gray-900">{{ $stats['total_projects'] }}</div>
                    <div class="text-sm text-gray-600">Total Proyek</div>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <div class="flex items-center">
                <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mr-4">
                    <i class="fas fa-play text-green-600 text-xl"></i>
                </div>
                <div>
                    <div class="text-2xl font-bold text-gray-900">{{ $stats['active_projects'] }}</div>
                    <div class="text-sm text-gray-600">Proyek Aktif</div>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <div class="flex items-center">
                <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mr-4">
                    <i class="fas fa-check text-purple-600 text-xl"></i>
                </div>
                <div>
                    <div class="text-2xl font-bold text-gray-900">{{ $stats['completed_projects'] }}</div>
                    <div class="text-sm text-gray-600">Proyek Selesai</div>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <div class="flex items-center">
                <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center mr-4">
                    <i class="fas fa-money-bill text-yellow-600 text-xl"></i>
                </div>
                <div>
                    <div class="text-2xl font-bold text-gray-900">Rp {{ number_format($stats['total_budget'], 0, ',', '.') }}</div>
                    <div class="text-sm text-gray-600">Total Budget</div>
                </div>
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <!-- Main Content -->
        <div class="lg:col-span-2">
            <!-- Institution Details -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 mb-6">
                <div class="p-6">
                    <h2 class="text-xl font-semibold text-gray-900 mb-4">Informasi Institusi</h2>
                    
                    <div class="space-y-4">
                        <!-- Basic Info -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <h3 class="text-sm font-medium text-gray-700 mb-1">Nama Institusi</h3>
                                <p class="text-gray-900">{{ $institution->name }}</p>
                            </div>
                            <div>
                                <h3 class="text-sm font-medium text-gray-700 mb-1">Tipe</h3>
                                <span class="px-2 py-1 bg-blue-100 text-blue-800 text-sm font-medium rounded">
                                    {{ ucfirst($institution->type) }}
                                </span>
                            </div>
                        </div>

                        @if($institution->address)
                            <div>
                                <h3 class="text-sm font-medium text-gray-700 mb-1">Alamat</h3>
                                <p class="text-gray-900">{{ $institution->address }}</p>
                            </div>
                        @endif

                        <!-- Contact Info -->
                        @if($institution->contact_person || $institution->phone || $institution->email)
                            <div>
                                <h3 class="text-sm font-medium text-gray-700 mb-2">Informasi Kontak</h3>
                                <div class="bg-gray-50 rounded-lg p-4">
                                    @if($institution->contact_person)
                                        <div class="flex items-center mb-2">
                                            <i class="fas fa-user text-gray-400 w-5"></i>
                                            <span class="text-gray-900">{{ $institution->contact_person }}</span>
                                            @if($institution->contact_position)
                                                <span class="text-gray-500 ml-2">({{ $institution->contact_position }})</span>
                                            @endif
                                        </div>
                                    @endif
                                    @if($institution->phone)
                                        <div class="flex items-center mb-2">
                                            <i class="fas fa-phone text-gray-400 w-5"></i>
                                            <a href="tel:{{ $institution->phone }}" class="text-blue-600 hover:text-blue-800">
                                                {{ $institution->phone }}
                                            </a>
                                        </div>
                                    @endif
                                    @if($institution->email)
                                        <div class="flex items-center">
                                            <i class="fas fa-envelope text-gray-400 w-5"></i>
                                            <a href="mailto:{{ $institution->email }}" class="text-blue-600 hover:text-blue-800">
                                                {{ $institution->email }}
                                            </a>
                                        </div>
                                    @endif
                                </div>
                            </div>
                        @endif

                        @if($institution->notes)
                            <div>
                                <h3 class="text-sm font-medium text-gray-700 mb-2">Catatan</h3>
                                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-3">
                                    <p class="text-sm text-yellow-800">{{ $institution->notes }}</p>
                                </div>
                            </div>
                        @endif
                    </div>
                </div>
            </div>

            <!-- Projects List -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200">
                <div class="p-6">
                    <div class="flex items-center justify-between mb-4">
                        <h2 class="text-xl font-semibold text-gray-900">Proyek Terkait</h2>
                        <span class="text-sm text-gray-500">{{ $institution->projects->count() }} proyek</span>
                    </div>
                    
                    @if($recentProjects->count() > 0)
                        <div class="space-y-4">
                            @foreach($recentProjects as $project)
                                <div class="border border-gray-200 rounded-lg p-4 hover:bg-gray-50 transition-colors duration-200">
                                    <div class="flex items-start justify-between">
                                        <div class="flex-1">
                                            <h3 class="text-sm font-medium text-gray-900 mb-1">
                                                <a href="{{ route('projects.show', $project) }}" class="hover:text-blue-600">
                                                    {{ $project->name }}
                                                </a>
                                            </h3>
                                            @if($project->description)
                                                <p class="text-sm text-gray-600 mb-2">{{ Str::limit($project->description, 100) }}</p>
                                            @endif
                                            <div class="flex items-center space-x-4 text-xs text-gray-500">
                                                <span><i class="fas fa-user mr-1"></i>{{ $project->client_name }}</span>
                                                @if($project->budget)
                                                    <span><i class="fas fa-money-bill mr-1"></i>Rp {{ number_format($project->budget, 0, ',', '.') }}</span>
                                                @endif
                                                @if($project->deadline)
                                                    <span><i class="fas fa-calendar mr-1"></i>{{ $project->deadline->format('d M Y') }}</span>
                                                @endif
                                            </div>
                                        </div>
                                        <div class="ml-4">
                                            @if($project->status)
                                                <span class="px-2 py-1 bg-blue-100 text-blue-800 text-xs font-medium rounded">
                                                    {{ $project->status->name }}
                                                </span>
                                            @endif
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                        
                        @if($institution->projects->count() > 5)
                            <div class="mt-4 text-center">
                                <a href="{{ route('projects.index', ['institution' => $institution->id]) }}" 
                                   class="text-blue-600 hover:text-blue-800 text-sm">
                                    Lihat semua proyek ({{ $institution->projects->count() }})
                                </a>
                            </div>
                        @endif
                    @else
                        <div class="text-center py-8">
                            <div class="w-16 h-16 mx-auto mb-4 text-gray-400">
                                <i class="fas fa-project-diagram text-4xl"></i>
                            </div>
                            <h3 class="text-lg font-medium text-gray-900 mb-2">Belum ada proyek</h3>
                            <p class="text-gray-500 mb-4">Institusi ini belum memiliki proyek yang terdaftar.</p>
                            <a href="{{ route('projects.create', ['institution_id' => $institution->id]) }}" 
                               class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors duration-200">
                                Tambah Proyek Pertama
                            </a>
                        </div>
                    @endif
                </div>
            </div>
        </div>

        <!-- Sidebar -->
        <div class="lg:col-span-1">
            <!-- Quick Actions -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Aksi Cepat</h3>
                <div class="space-y-3">
                    <a href="{{ route('projects.create', ['institution_id' => $institution->id]) }}" 
                       class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-lg transition-colors duration-200 flex items-center">
                        <i class="fas fa-plus mr-2"></i>
                        Tambah Proyek
                    </a>
                    <a href="{{ route('institutions.edit', $institution) }}" 
                       class="w-full bg-gray-600 hover:bg-gray-700 text-white py-2 px-4 rounded-lg transition-colors duration-200 flex items-center">
                        <i class="fas fa-edit mr-2"></i>
                        Edit Institusi
                    </a>
                    <a href="{{ route('institutions.index') }}" 
                       class="w-full text-gray-600 hover:text-gray-800 py-2 px-4 text-center border border-gray-300 rounded-lg transition-colors duration-200">
                        Kembali ke Daftar
                    </a>
                </div>
            </div>

            <!-- Institution Stats -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Detail Institusi</h3>
                <div class="space-y-3">
                    <div class="flex justify-between">
                        <span class="text-sm text-gray-600">Status</span>
                        <span class="text-sm font-medium text-gray-900">
                            {{ $institution->is_active ? 'Aktif' : 'Tidak Aktif' }}
                        </span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-sm text-gray-600">Tipe</span>
                        <span class="text-sm font-medium text-gray-900">{{ ucfirst($institution->type) }}</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-sm text-gray-600">Terdaftar</span>
                        <span class="text-sm font-medium text-gray-900">{{ $institution->created_at->format('d M Y') }}</span>
                    </div>
                    @if($institution->updated_at != $institution->created_at)
                        <div class="flex justify-between">
                            <span class="text-sm text-gray-600">Terakhir Diperbarui</span>
                            <span class="text-sm font-medium text-gray-900">{{ $institution->updated_at->format('d M Y') }}</span>
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>
@endsection